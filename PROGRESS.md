# 五子棋项目进度

**交付物**：`/Users/yhma/Documents/gomoku/index.html`（单文件、零依赖、双击可玩）
**状态**：可正常游玩。VCT 加了置换表（三档 A/B 实测**中性**，保留但不构成棋力提升，见「一之三」），
其余引擎参数不变；新增「接管 AI」模式、引擎分析默认隐藏。

---

## 一、已完成并通过验证

### 1. 引擎核心
| 模块 | 说明 |
|---|---|
| 棋型识别 | 用递归定义（活三=再落一子成活四）预计算 `4^8` 邻域查找表，开局约 20ms 建表，查棋型=一次数组读 |
| 增量评估 | 每落一子只重算受影响的 4 条线上 32 个点 |
| 主搜索 | PVS + Zobrist 置换表 + 杀手着法 + 历史启发 + 后期着法削减 + 冲四延伸 |
| VCF | 连续冲四算杀，防守方单点被迫应招 |
| VCT | 冲四+活三混合威胁算杀，能找到四三杀/双三杀 |
| 禁手规则 | 长连/四四/三三，引擎与界面共用同一套判定（深层嵌套用实用近似） |

### 2. 界面
- 棋盘渲染、坐标、星位、手数、最后一手红环（沿棋子外缘，不遮数字）
- 悔棋、求助 AI、AI 自战、5 档难度、思考时长、禁手开关
- **顶部胜率条**：左橙=AI / 右蓝=你，两侧恒和 100%，搜索中实时跳动并显示「计算中…」
- 标题已改为「五子棋」（去掉「地狱级」）
- 非 Worker 环境（如 Safari 直开 `file://`）自动回退主线程，单步钳制到 3 秒并显示提示

### 3. 验证结果
| 验证项 | 结果 |
|---|---|
| 棋型判定（活三/冲四/活四/五/跳三等） | 12/12 正确 |
| 增量评估 vs 从零重建（40 组随机对局） | 逐字节一致，反悔到空盘无残留 |
| 综合分缓存 vs 直算（5963 个空点） | 完全一致 |
| 战术（挡冲四/挡活三/成五） | 3/3 |
| 自战健壮性（含禁手模式） | 无非法落子、无重复落子、正常终止 |
| **速度优化** | 43~83k/s → **368k/s**（约 5~8 倍），同预算深度 8→11 层 |
| **VCT 功能性** | 纯双三局面：VCF 算不出，VCT 用 13 节点找到杀点 H8 |
| **VCT 可靠性（对抗验证）** | 宣称必杀 3 次，换独立强搜索防守后 3 次都真赢，**0 次假杀** |
| 浏览器端到端 | Worker 启动、对弈、胜负判定、悔棋、求助、禁手判负、胜率条全部正常 |

### 4. 胜率条的标定（非拍脑袋）
sigmoid 常数 K 原本凭感觉取 22000，实测严重过度自信。跑 14 局自战采集 296 个样本，统计「评分 → 最终胜负」相关性后最小二乘拟合得 **K=43500**：

| 评分区间 | 实测胜率 | K=22000 预测 | K=43500 预测 |
|---|---|---|---|
| < -40000 | 23.9% | 3.1% | 14% |
| -2000 ~ 2000 | 46.8% | 49.8% | 50% |
| > 40000 | 78.1% | 97.7% | 86% |

另外把非杀棋分数的显示上限压到 98%——启发式「近赢」和搜索证实的杀棋是两回事，只有后者才显示 100%。

---

## 一之二、2026-08-06：测量方法修复 + VCT 结案

### ⭐ 先修测量：旧的 A/B 赛制读的全是噪声
两个**字节级完全相同**的引擎，用旧赛制（1.5s 时间制，6 开局双色 12 局）打出 **4 : 7**。
也就是说旧结论里「4:8」这个量级的差距**完全在噪声内**——包括当时判定「VCT 净负」的那一局数据。

两个随机源，都已消除（仅在实验副本中，`index.html` 引擎未改）：
1. **墙钟抖动**：所有超时判断都走 `nowMs()`。把它换成节点计数驱动的虚拟时钟
   （`TICKS/TRATE`，`pvs`/`vcf`/`vct` 入口各 `TICKS++`），12%/28%/45% 的配额逻辑一行不用改。
2. **`reset()` 不清历史启发/杀手表**（`index.html:317`，`history` 是全局的）。跨局残留
   造成路径依赖，同一开局跑两遍会走出不同的棋。开局前必须 `history.fill(0)` + `killers.fill(0)`。

修复后：同一局跑两遍着法逐字节相同；**相同引擎对照赛 = 精确 6 : 6，噪声底归零**。

### ✅ VCT 结案：在**出厂配置**下是中性的，原判「净负」是噪声。引擎参数已全部还原

**结论：VCT 不是问题，不用再查了。** 旧记录里那条「最高优先级：VCT 净负收益 4:8」，
其证据强度低于当时的噪声底（相同引擎 4:7），不成立。

用各档**真实出厂配置**重测（确定性台子，随机开局双色）：

| 档位 | 预算 / VCT 设定 | VCT关 vs VCT开 | 结论 |
|---|---|---|---|
| 3 困难 | 1.5s, `vctDepth:5`, 无防守过滤 | **24 : 24**（48局） | 中性 |
| 4 大师 | 4s, `7 / 5` | **12 : 12**（24局） | 中性 |
| 5 地狱 | 9s, `9 / 7` | 未测（单局太慢） | — |

因此 `LEVELS` 中的 `vctDepth` / `vctDefDepth` **全部保持原值**，`index.html` 引擎参数与本轮之前一致。

**⚠️ 一个必须记住的教训（差点写进代码）**：中途曾据「VCT 全开 vs 全关 = 18:30」把 VCT
全部关掉，并声称 +90 Elo。**那组数据用的是 level-4 设定跑在 1.5 秒下——这个组合没有任何
一个难度档在用。** 换成真实出厂配置后差异归零。跑 A/B 时**配置必须与实际出厂值逐项一致**，
否则测的是一个不存在的引擎。

（附：那组不存在配置下的拆分数据仍有参考价值——全开 18:30、只进攻 22:26、只防守 20:27，
说明 VCT 的成本是固定时间比例、收益随预算增长，预算越小越不划算。）

### ✅ 根过滤「未检查=安全」偏置：真缺陷，但对棋力无可测影响
- 触发机制与旧记录不同：**不是 45% 超时 break**（120 次调用中只有 11 次超时，且 4 次
  病态全部未超时）。真正原因是 `index.html:828` 的 `lim=cnt<14?cnt:14` 与 `goodLeft=cnt`：
  `cnt` 是 26 而只查前 14 个，兜底条件 `goodLeft<=0` 把 12 个从没查过的着法算作「好」，
  于是永远不触发，搜索被关进第 15~26 名的着法里选。
- 实测触发频率：120 次根过滤调用中 4 次，**全部发生在执白防守方**，其中 3 次在输棋前 2~4 手。
- 但最小修复（兜底条件改为「已检查的着法里没有一个干净」）在确定性台子上打出 **6 : 6**，
  与相同引擎对照完全一致 → **对棋力无可测量影响，不是失分主因**。未合入 `index.html`。

### ❌ 试过但无收益的改动（别再重复试）
| 改动 | 结果 | 说明 |
|---|---|---|
| 根过滤兜底缺陷修复 | 6 : 6 (12局) | 真缺陷，但无可测影响 |
| 关掉整个根过滤 | 23 : 25 (48局) | 中性，留着不亏拆掉不赚 |
| **置换表跨步保留**（去掉 `think()` 里的 `ttClear()`） | 22 : 26 (48局) | **无收益，略偏负** |
| **VCT 置换表** | LV3 24:24(48局)、LV4 9:15(24局)、LV5 11:12和1(24局) | **三档合看中性**，LV4 的 +6 是噪声；代码已合入且无害，保留，但不算收益（详见「一之三」） |
| **根着法按上层分数重排**（`rootsort`，LV5） | 26 : 21 和 1 (48局) = **0.73σ** | **未能证明有收益**；机制确实生效（插桩见多一层深度）但没兑现成棋力（详见「一之四」） |
| **杀棋分 ply 校正 + TT 跨步保留**（`mateplykeep`，LV5） | **98 : 99 和 3（200 局预登记）= −0.07σ** | **中性**。第一轮 48 局曾打出 29:19（1.44σ），200 局证明那是噪声（详见「一之六」） |

TT 跨步保留是教科书级建议，这里失效的可能原因：本引擎存分数时**未按 ply 校正杀棋分**
（`index.html:571` 直接 `ttVal[ti]=best`），换根后杀棋距离即失真；深度字段也是相对旧根的。
每步清空可能是有意为之。**若要重试这条，必须先做杀棋分的 ply 校正。**

### 📌 本轮棋力净变化：**0**
三条文献直觉（根过滤有用、TT 跨步保留白送、VCT 净负）全部被数据否掉。
真正的产出是**测量台**——在此之前，这三条大概率会被当成「优化」合进代码并自我感觉良好。

---

## 一之三、2026-08-07：VCT 加置换表——已实现并合入，但**实测中性**（结论见本节末）

### ✅ 只给 VCT 加 TT，不碰 VCF
前置测量（`scratchpad/probe_tt.js`，见「一之二」下方待办）已经把方案定死：VCF 只占算杀节点
0.3%，加 TT 白写；VCT 置换率 86%，深度字段可以照旧存但不影响命中率。真正要解决的是
「中止污染」和「碰撞校验」两个坑，不是要不要做。

### 实现（`index.html`：新增 `vctTt*` 全局数组 + `vct()` 改写 + `ttClear()` 顺带清表）
- 独立的 VCT 置换表（`vctTtKey/vctTtSide/vctTtDepth/vctTtFlag/vctTtMove`，2^21 项），跟主搜索
  TT（`ttKey` 等）分开存，互不干扰。
- **深度字段要比对**——不是因为它能区分局面（同一局面剩余深度本是常数），而是因为同一手
  `think()` 内，进攻搜索（`cfg.vctDepth` 起步）和 `rootFilter` 防守搜索（`cfg.vctDefDepth`
  起步）共享同一张表；两条起始预算不同的线如果算到同一个真实局面，`depth` 字段的值并不相等
  （相差一个常数偏移），不比对会把两条线的结果错误互换，是正确性问题，不只是「多一层保险」。
- **中止污染**：新增全局计数器 `vctAbortMark`。`vctLimit`/`vctDeadline`/`lvl>=30` 撞到时自增
  并直接返回 0，不写表；节点收尾时只有「子树搜索期间计数器没变过」才允许写证伪条目。证明
  （找到杀点）不受此限，随时可安全写——中止的子节点只会被上层当成「防住了」处理，不会导致
  误判为找到杀点。
- **碰撞校验**：证明条目命中后先校验 `board[m]===EMPTY && forcingKind(side,m)!==0`，没通过
  就当 miss 正常重算，防 20 位索引 + 32 位校验和仍会有的哈希碰撞把引擎带偏成一步垃圾「必胜」棋。
- `ttClear()` 顺带清 VCT 表——跟主搜索 TT 一样「每步清空」，口径对齐置换率的测量口径（单步内
  86%），且 `ENG.clearHistory()` 本来就走 `ttClear()`，跨局残留问题不需要额外改动就解决了。

### 验证
| 验证项 | 结果 |
|---|---|
| 确定性（加 TT 后） | LV3/LV4/LV5 出厂配置、多组开局，两遍逐字节相同 |
| 同引擎对照（噪声底） | LV3 6:6、LV4 6:6、LV5 6:6（各 12 局），干净 |
| A/B：LV3 出厂配置，TT关 vs TT开 | **24 : 24（48 局）——中性** |
| A/B：LV4 出厂配置，TT关 vs TT开 | 9 : 15（24 局）——TT 开 +6 |
| A/B：LV5 出厂配置，TT开 vs TT关 | **11 : 12 和 1（24 局）——中性** |

### ⚠️ 结论修正：三档合起来看，TT 的收益证据**不成立**

LV4 那个 9:15 当时就标注过只有约 1.2 个标准差、不算显著。后来补测 LV5（`ab_lv5.js` 的
前身、用户本机 8 进程并行，26 分钟跑完 36 局）拿到 **11:12 和 1，中性**。

这直接推翻了当初给 LV4 编的解释。原话是「LV4 的 `rootFilter:true` + `vctDefDepth:5` 让攻防
两条线共享同一张表，所以收益兑现」——但 **LV5 的结构完全同构**（`rootFilter:true`、
`vctDefDepth:7`，预算还翻倍），按这个解释 LV5 应该收益**更大**才对，实测是中性。

所以诚实的结论是：**VCT 置换表在三个档位都是中性的，LV4 的 +6 是噪声**，那段「攻防两条线
共享」的机制解释是先看到结果再编出来的故事，不是经过检验的机制。这正是本项目反复踩的同一个
坑（见「一之二」的 +90 Elo 教训）——**24 局的样本量不足以支撑「有收益」的断言，只够排除
「有大幅负收益」**。

**代码去留**：`index.html` 里的实现**保留不动**。理由是它在三档都不负、实现本身正确（中止
污染与碰撞校验都处理了），拆掉是纯粹的改动风险换零收益。但**不得再宣称它提升了棋力**——
上面「状态」栏和本节标题的措辞已按此修正。

（脚本：`scratchpad/ab_vcttt.js` 单进程版、`ab_vcttt_parallel.js` 多进程版 + `ab_worker.js`。
先在 `load.js` 加 `vcttt` 变体验证再手工合入 `index.html`；`vcttt` 变体从此过期——`load.js`
靠原文比对识别改写点，`index.html` 一改就报错，是设计内行为。合入后要再测「有 TT vs 没 TT」
请用反向变体 `noTT`。）

---

## 一之四、2026-08-07：地狱级（LV5）——找到真瓶颈，但修复**未能证明有收益**

目标转向「只加强 5 档」后，先做的不是改代码而是量预算去向。**LV5 单手只用掉 56~74% 的
9 秒预算就收工，`maxDepth` 写 18 实际只搜到 12~13 层。**（`node -e` 插桩，3 个中局局面。）

翻迭代加深那段（`index.html:885`）找到三处浪费，其中一处是真问题：

### 🔴 A. 根节点着法从不重排（`sortRoot()` 被架空）
`sortRoot()` 定义在 `index.html:817`，全文件**只在「弱难度随机」那个分支里被调用过一次**
（`index.html:904`），迭代加深循环里根本没调。也就是说 18 层迭代，根着法顺序一直是最初
`genMoves` 的启发排序，**上一层搜出来的分数从不用于给下一层排序**。这是教科书级的浪费：
把上层最优着法排到最前面搜，alpha-beta 的剪枝效率会显著提高，层数越深收益越大。

修复就是循环末尾加一句 `sortRoot();`（变体 `rootsort`）。插桩实测：3 个局面里 1 个多搜出
一层（d12→d13），预算使用率从 56/59/74% 提到 66/76/67%。

### ⚪ B、C. 另外两处：单独用都是空操作
- `deeper`（收尾阈值 `remain*0.45` → `0.65`）：预算能用满到 100%，但**深度不涨**——
  多搜的那层超时后被整层丢弃，最终还是同一步棋。
- `partial`（采纳超时那层已搜完部分的结果，去掉 `!timeout` 条件）：跟出厂**逐字节相同**——
  45% 阈值下搜索是干净收工的，压根没有半层结果可捡。

两者理论上应该配对（`deeppartial`：放宽阈值去够更深一层 + 把超时那层捡回来），但插桩显示
**开了 `rootsort` 之后 `lv5all` 与 `rootsort` 结果完全一致**，说明此时循环不是卡在那个阈值
上退出的，B/C 失效。故实际值得测的只有 `rootsort`。

### A/B 结果：`rootsort` **未能证明有收益，不合入**

`scratchpad/ab_lv5.js`，LV5 出厂配置，用户本机 8 进程并行，47 分钟跑完 60 局：

| | 结果 |
|---|---|
| 对照 `orig` vs `orig2` | **6 : 6（12 局）** —— 台子干净 |
| **`rootsort` vs 出厂** | **26 : 21 和 1（48 局）** |

47 局分胜负、赢 26。零假设下期望 23.5、标准差 √47/2 ≈ 3.43，
即 **z ≈ 0.73σ，双侧 p ≈ 0.47**——完全落在噪声里。

**判定依据必须前后一致**：本文件刚刚才用 1.22σ 的理由撤回了 LV4 的「TT 有收益」结论
（见「一之三」）。既然 1.22σ 都不算数，**0.73σ 更不能反过来宣称有收益**。故按「四、实验台」
的硬规矩：A/B 不为正 → 不合入，记入「试过但无收益」表。

**这里要分清两件事，插桩测到的都是真的**：`sortRoot()` 确实从未在迭代加深里被调用（真
遗漏）；加上之后确实多搜出一层、预算使用率也上去了（机制确实生效）。但**「搜得更深」没有
兑现成可测量的棋力**。这已经是本项目第四次出现同一模式（根过滤兜底、TT 跨步保留、VCT 置换表、
现在的 rootsort）——**「代码里确实有个真问题」不等于「修好它就会变强」，两者之间隔着一次实测。**

### 若要继续追这条：需要的是样本量，不是新解释
点估计 26/47 = 55.3%（≈ +37 Elo），但 95% 置信区间大致横跨 40%~70%（≈ −70 ~ +145 Elo），
**这个实验设计根本分辨不了这个量级的效应**。48 局只够排除大幅负收益。
- 要把 +37 Elo 这个量级测出显著，需要**数百局**（用户机器约 47s/局 × 8 并行 → 400 局约 5 小时，
  一个通宵可行）。
- 更省算力的路子：这类「改善着法排序 → 多搜一层」的改动，**直接测搜索效率**（固定预算下达到的
  深度、或达到固定深度所需节点数）比测胜负的方差低一个量级。缺点是效率指标不一定线性换算成棋力。
- **不要做的事**：看到 26:21 偏正就补跑几十局直到显著——那是 p-hacking，正是本文件反复警告的坑。

---

## 一之五、2026-08-08：界面重做（与棋力实验完全解耦）

引擎源码**一字未动**——每次界面提交前都跑过逐字节比对（取 `<script id="engineSrc">`
到其 `</script>` 之间的原文与上一提交对比）。这条必须一直守住：`scratchpad/load.js`
是靠原文匹配来抽取和改写引擎的，引擎一改，全部实验脚本立刻报错失效。

**做了什么**：响应式布局（窄屏把按钮卡排到设置卡之前、首屏收起设置）、中英切换
（`data-i18n` + `t(key)`，语言存 localStorage）、可访问性修复（允许缩放、开关补
`role=switch`/键盘操作、快捷键在输入控件获焦时不抢键）、favicon 与分享预览标签、
液态玻璃观感、胜率驱动的水粉背景、点击涟漪/开关弹簧/折叠高度动画。

### 界面这边踩到的两个坑（都由实测定位，不是猜的）

1. **背景动画会拖垮整个页面**。第一版给背景光斑加了缓慢漂移，桌面 16.7ms → 83ms、
   手机 → 33ms。原因不是玻璃本身贵，而是**背景每帧都在变，就意味着上面每块玻璃
   每帧都要重算一次 backdrop**。对照实验：只关动画 → 手机回到满帧；只关
   backdrop-filter → 桌面才 83→66。所以定的规矩是**限制背景重绘频率**
   （现在静止 14fps、胜率变化时 24fps），而不是放弃动效。
2. **靠全屏黑纱保证文字对比度，会把背景一起压灰**。改成把暗底加进玻璃自身的背景层
   （`--glass-bg` 叠一层 `rgba(7,11,19,.46)`），背景才能放开饱和。

### 背景的可调参数（都在 `index.html` 的 `BG` 那段）
| 参数 | 作用 |
|---|---|
| `WARM`/`COOL` | 色团色板（高饱和） |
| `WARM_BASE`/`COOL_BASE` | 底层渐变的深色版，负责铺满 |
| `SOFT` | 交界调和带的宽度，越大越糊 |
| `edgeAt()` 里两项 sin 的系数 | 分界线的起伏幅度与流动速度 |
| 色团的 `a` | 单团浓度 |
| `t+=dt*1.15` | 整体漂移速度 |
| `gap=moving?42:70` | 重绘间隔（**动这个之前先看上面第 1 条**） |

**分界线为什么不会把颜色割裂**：它是 `x = edgeAt(y)` 的单值曲线，对每个 y 只有一个
取值，所以「曲线左边全暖、右边全冷」恒成立，两种颜色各自始终连通，不会碎成色块。

### ⚠️ 界面自测脚本**没有入库**，需要时得重写
曾经写过 `regress.mjs`（三视口功能回归）、`perf.mjs`/`perf3.mjs`（帧率）、`inter.mjs`
（交互动效）、`bgtest.mjs`（背景随胜率变色）、`transtest.mjs`（过渡），但**这些文件从未
提交，仓库里一个都没有**——别照着这段去 `scratchpad/` 里找，会扑空。

重写的话套路很短：`npm i playwright`（浏览器用现成的 `/opt/pw-browsers/chromium`，
**不要跑 `playwright install`**）+ `python3 -m http.server 8765`，然后 `page.goto` 各视口截图、
读 `document.documentElement.scrollWidth` 判溢出。**跑完记得删掉 `node_modules/`、
`package.json`、`package-lock.json`**——`.gitignore` 没盖这三个，留着会被误提交。

### 2026-08-08 上传前的界面复验（结论：已达标，不再优化）
headless Chromium 四视口 1440×900 / 820×1180 / 390×844 / 320×568：

| 检查 | 结果 |
|---|---|
| 横向溢出 | 四档全部 `scrollWidth === clientWidth`，越界元素 0 |
| console / pageerror | 0 |
| 棋盘自适应宽度 | 638 → 560 → 342 → 264px，320px 下仍完整可读 |
| 功能烟测 | 落子 H8 → AI 应手 I7 → 胜率条 49/51 → `U` 悔棋清空，全通 |
| 键盘可达性 | 15 个可聚焦元素；开关 Enter 后 `aria-checked` 正确翻转 |
| i18n | `<html lang>` 与 `document.title` 随语言切换，不只换可见文字 |
| `prefers-reduced-motion` | 生效 |

**已知且**故意**不修的一处**：桌面 1440×900 下页面高 962px，有约 60px 轻微滚动，底部提示卡
被裁一角。修法是给棋盘加 `min(…, 100vh - X)` 高度上限，但属纯口味，改动要重跑整套四视口
回归，收益不成比例。

---

## 一之六、2026-08-10：杀棋分 ply 校正——**预登记 200 局，中性**（本项目第一次预登记实验）

### 起点：一个真实存在、但几乎不改变走法的 bug

`pvs()` 里杀棋分是 `WIN-ply`，`ply` 从**根**算起。存进 TT 时直接 `ttVal[ti]=best`，
存的是「从根数第几层将杀」而不是「距这个节点还有几步将杀」。同一局面在**不同 ply**
被置换命中时，取回的杀棋距离就偏了。

**先测频率再动手**（`scratchpad/probe_mate.js`，给 TT 加影子数组记录写入时的 ply）：

| 指标 | LV5 两局（44 手） |
|---|---|
| TT 写入 | 1644 万次，其中杀棋分 27.4% |
| TT 命中且用了表值 | 330 万次，其中取回杀棋分 25.5% |
| **取回杀棋分且 ply 不一致（值是错的）** | **270 次 = 0.008%** |
| **错值真的被返回** | **267 次** |
| **杀棋距离最大偏差** | **10 步** |

**bug 是真的**（267 次真的把错误的杀棋距离返回给了上层，最狠错 10 步），
**但频率只有 0.008%**。而且单独修它在 LV3/LV4 六个开局下与出厂**逐字节相同**——
因为错的杀棋分**仍然是杀棋分**，照样压过一切非杀棋分，选出来还是同一步棋。

### 真正值得测的是它解锁的那条线

每步 `ttClear()` 让条目活不过一手棋，所以 ply 错位只能来自「同一手棋内的偶发转置」。
**一旦 TT 跨步保留，上一手存的条目在这一手会系统性地差 2 个 ply**，bug 从偶发变成
每条跨步复用的杀棋分都错。PROGRESS「一之二」正是这么猜的，并写明「若要重试 ttkeep，
必须先做杀棋分的 ply 校正」。变体 `mateplykeep` 就是去验这条猜测。

### 结果：第一轮看着有戏，200 局证明是噪声

| 轮次 | 对照 | `mateplykeep` vs 出厂 | z |
|---|---|---|---|
| 第一轮（探索，seed 20260811） | 6:6 | 29 : 19（48 局） | **+1.44σ**，p=0.15 |
| **预登记重测（seed 20260812）** | **6:6** | **98 : 99 和 3（200 局）** | **−0.07σ**，p=0.94 |

预登记结果：胜率 **49.7%（≈ −2 Elo）**，95% CI **−51 ~ +47 Elo**。
**判定：未能证明 → 不合入 `mateplykeep`，且按预登记不得追加局数。**

### ⭐ 这一轮真正的产出是**预登记流程**，不是结论

第一轮的 29:19 折合 +73 Elo，很诱人。如果按「看着偏正就补跑到显著」走，
大概率会在某个中途点撞上 2σ，然后合入一个实际为零的改动——**这正是本文件反复警告、
却一直只能靠自律的坑**。这次把规则写进 `scratchpad/PREREG_mateplykeep.md` 并**在数据
产生之前提交**（提交哈希即时间戳），事后无法改口径。200 局的答案是精确的 50%。

「1.44σ 不算数」这条判断因此得到了事后验证——**不是保守，是对的**。

### 执行中踩的两个坑（都写进预登记的补充条款了）

1. **后台长任务会被清理杀掉**（实测撑约 4.6 小时）。更狠的是第一次执行跑到 180/212 局
   时**数据全废**——日志只记了手数和耗时，**胜负只存在主控进程内存里**，等全部跑完才打印。
   挂了定期存盘也没用，存的是一堆无法判定的行。
   **教训：存盘要存「判定所需的字段」，不是「看起来在增长的日志」。**
2. 改成分块跑之后又发现：日志没记 `job.id`，被杀后**无法确定已完成的是哪几局**，只能整块重来。
   补上 `job.id` + 参数指纹（seed/开局区间）后，被杀的代价从「丢两小时」降到「丢几分钟」。

**分块为什么不算优化停止点**：块边界是事先定死的下标切片，与数据无关，
已验证四块拼回来与预登记的 100 个开局完全一致、无重叠。这跟「看到比分偏正就追加局数」
有本质区别——后者的停止点由数据决定。

### 🔴 顺带查出：历史上那条 `ttkeep 22:26` 是**无效数据**

核对 `scratchpad/run3.js` 发现，当年 ttkeep 打出 22:26 用的配置是

```js
{ timeMs: 1500, maxDepth: 12, vcfDepth: 14, vctDepth: 0, vctDefDepth: 0, rootFilter: true }
```

`timeMs` 是 LV3 的、`maxDepth`/`vcfDepth`/`rootFilter` 是 LV4 的，而 `vctDepth: 0`
意味着 **VCT 被整个关掉**——没有任何一个难度档是这个组合。按硬规矩之二，**这条数据无效**，
与「一之二」记载的 +90 Elo 是同一类错误的另一个实例。

它恰好是「TT 跨步保留略偏负、需要 ply 校正来救」这条推理的**唯一起点**。因此：
- **不得**声称「ply 校正让 ttkeep 从 22:26 翻成 29:19」——两次实验配置、档位、VCT 开关全不同，根本不可比。
- 「试过但无收益」表里那条 22:26 已标注为**配置无效**。

### 合入了什么

**只合入 ply 校正本身**（`index.html` 三处：`MATE_BAND` 常量、读表 `-ply`、写表 `+ply`），
理由是**正确性**——引擎确实在返回错误的杀棋距离。合入前后跑过逐字节等价验证（LV3/LV4 六个开局
走法完全相同），**它是空操作，不改变任何一步棋**。

**⚠️ 不得宣称它提升了棋力。** 配套的 200 局预登记实验结果是 −2 Elo。

`load.js` 里 `mateply` 变体随合入过期（改写点不再匹配，`build` 会报错，是设计内行为）；
要重测「有没有 ply 校正」用反向变体 **`noMatePly`**——跟当年 `vcttt` 合入后留 `noTT` 是同一套路。

---

## 二、未完成 / 待解决

### 1. 旧结论已过期，需重测
早先「地狱级 10:2 胜困难级」是在**加速与 VCT 改造之前**测的，不代表当前版本棋力。

### 2. LV5 还没测过的候选
`rootsort` 已测（0.73σ，未证明有收益，见「一之四」），剩下没测的：
- **`deeppartial`**（放宽收尾阈值 + 采纳半层结果）：变体已就绪、确定性已验证，但没跑过 A/B。
  注意插桩显示它在 LV4 某个开局下会改变走法（`lv5all` 58 手 vs `rootsort` 19 手），
  所以**不能因为「在那 3 个 LV5 局面里看着和 rootsort 一样」就跳过实测**。
- 主搜索里一直没做的标准技术：aspiration window、反着法启发（counter-move）、null move。
- 杀棋分的 ply 校正——它自身是正确性问题，且是重试「TT 跨步保留」的前置条件（见「一之二」）。

### 3. 其他未做
- 无开局库
- 主搜索未加 aspiration window、反着法启发（counter-move）、null move

### 4. 只剩用户本人能做的两件事（**不要替他决定**）
- **仓库可见性**：`main` 已经带上 VCT 置换表 + 新界面 + README（PR #1 已合），但仓库仍是
  私有。开 GitHub Pages 还差两步：仓库改公开 + Settings→Pages 选 `main` / `(root)`，
  **这两步只能他本人在 GitHub 设置里点**。
- **README 的许可证一节是空的**。加 `LICENSE` 需要他的署名，不该代填。

---

## 六、下一个窗口从这里接手

**当前状态**：`claude/vct-substitution-table-ab-zz5vci` 已经过 PR #1 合进 `main`，
`main` 就是最新版本。后续新工作请从 `main` 开新分支，**不要再往这条已合并的分支上堆提交**。

**已完成**：引擎侧 VCT 置换表（实测中性、保留但不算收益）；界面全套重做（见「一之五」，
2026-08-08 已做上传前四视口复验，结论是达标、不再优化）。

**最近一次结论**：LV5 的 `rootsort` 48 局 26:21（0.73σ），未达标、未合入，已记入
「试过但无收益」表。若还想追这条，需要的是**数百局的样本量**，不是新解释——
而且看到偏正就补跑到显著属于 p-hacking，本文件反复警告过。

**接手时最容易犯的错**（按危险程度排）：
1. 改 `index.html` 的引擎段。改了 `scratchpad/` 全部脚本立刻失效，且历史 A/B 数据
   不再可比。界面随便改，引擎段一个字符都别碰——改前后都跑一次逐字节比对。
2. 拿「诊断正确」当「有收益」。本项目四次改动全是真问题，修好后棋力净变化为 **0**。
   任何改动没跑过 A/B 就不算数。
3. 跑 A/B 前不跑同引擎对照（必须 6:6），或被测配置与 `LEVELS` 出厂值不一致
   （踩过一次，把不存在的引擎测出了 +90 Elo）。

---

## 三、界面新增（2026-08-06）

### 引擎分析默认隐藏
「引擎分析」卡片默认收起，点标题展开/收起（`showAnalysis` 标志）。同时联动隐藏
`think` 里的「策略：VCF 连四杀」提示——**主变化那栏会直接列出 AI 预判你怎么应,是最强的剧透**。
胜率条未动。

### 接管 AI 模式

右侧开关面板新增「接管 AI」（在「AI 自战」下方），开启后 AI 那一方由你落子：
- 你可以替 AI 落子，AI 不再自动应手；落完 AI 的子控制权回到你手上，两边都由你走
- 状态栏区分「轮到你：黑方」/「你接管 AI：白方」；悬停预览棋子颜色跟当前该谁走
- **悔棋只退一手**（平时退两手，因为要连 AI 的一起退）
- 「求助 AI」照常可用——可先问 AI 想走哪再决定
- 随时可开关；中途关掉时若正轮到 AI，它会立刻接手

实现：`takeOver` 标志 + `humanPlay` / `undo` / `draw` / `updateUI` / `newGame` 各一处分支。

---

## 四、实验台（重跑实验必读）

实验脚本已固化在 **`gomoku/scratchpad/`**（Node 直接跑，无依赖）：
- `load.js`——从 `index.html` 抽取引擎源码、注入虚拟时钟、生成变体
  （会校验源码未被改动，`index.html` 一改就直接报错，防止拿旧代码测新版本）
- `game.js`——对局器（**开局前会调 `clearHistory()`，这是确定性的前提**）
- `det.js`——确定性自检：同一局跑两遍，必须逐字节相同
- `ab.js` / `run.js`~`run5.js`——历次比赛与多臂实验，可作模板
- `diag.js`——根过滤插桩统计
- `probe_tt.js`——VCT 置换率测量（86% 结论的证据来源）
- `det_vcttt.js` / `det_vcttt2.js`——VCT 置换表专项确定性自检（LV3/LV4/LV5 出厂配置）
- `ab_vcttt.js`——VCT 置换表 A/B（LV3 48 局 / LV4 24 局，「一之三」结论的证据来源）；
  依赖的 `vcttt` 变体已随合入过期，重跑会报错（见「一之三」末尾）
- `eng_*.js` 是 `load.js` 生成的产物，可随时删

**两条硬规矩**：
1. 跑任何 A/B 之前先跑相同引擎对照，**必须 6:6**，否则说明还有别的随机源，结果不可信。
2. 被测配置**必须与 `LEVELS` 里的出厂值逐项一致**，否则测的是一个不存在的引擎（本轮踩过）。

虚拟时钟还有个副作用好处：引擎行为与真实耗时完全无关，**多个实验可以并行跑**，
CPU 抢占不再影响结果（旧记录里那条「两个进程抢 CPU 导致结论存疑」的保留意见已消除）。

**跑任何 A/B 之前先跑一次相同引擎对照，必须是 6:6，否则说明还有别的随机源，结果不可信。**

## 五、回滚提示

引擎参数为出厂原值（3/4/5 档 `vctDepth`/`vctDefDepth` = `5/0`、`7/5`、`9/7`）。
若要关闭 VCT，把它们置 0 即可——实测中性，关掉不影响棋力，但能省下 28% 的时间预算。
本轮界面改动（接管 AI、引擎分析隐藏）与引擎完全独立，删掉不影响对弈。

VCT 置换表（`vctTt*` 全局数组 + `vct()`/`ttClear()` 里的改写，见「一之三」）与 VCT 开关本身
是独立的两件事——`vctDepth` 置 0 只是不跑 VCT，不需要也顺带回滚置换表代码。若要单独回滚
置换表，把 `vct()` 换回不带 TT 的版本即可（`ttClear()` 也要去掉 `vctTtClear()` 那句），
出处见 `scratchpad/load.js` 里 `ORIG_VCT` 常量保存的合入前原文。
